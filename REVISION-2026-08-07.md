# Revisión automática de tasas y comisiones — 07-ago-2026

**Ejecución:** tarea programada `currencycommision-check` · **Alcance:** `CommissionTracker.html` + `commission-tracker-app`
**Resultado:** 5 hallazgos. **H1, H3, H4 y H5 aplicados** (aprobados). H2 pendiente de verificación manual.
**Estado git:** cambios escritos en el working tree, **sin commitear** — ver §5.

> **Corrección a la evaluación inicial de H3.** En la primera pasada escribí que "un número no
> auditado está decidiendo el ranking de una ruta entera". Es incorrecto. Al medir la sensibilidad,
> R4 pierde contra R3 **incluso con spread 0%** por debajo de ~US$5.000 (el wire de $15 solo ya la
> descarta) y arriba de ese monto necesitaría un spread ≤0,25% para ganar. El 4% mueve el número
> mostrado, no la recomendación. H3 baja de 🟠 a 🟡.

---

## 1. Comisiones verificadas contra fuente oficial — ✅ sin cambios

| Wallet | Valor en código | Verificación ago-2026 | Estado |
|---|---|---|---|
| Mercury ACH out | $0 | ACH (incl. same-day) gratis | ✅ |
| Mercury Wire intl | $15 | Wire USD intl gratis en SHA; $15 es la opción "OUR" | ✅ conservador |
| GrabrFi ACH out | 0.3% (mín $1, máx $5) | Idéntico — tarifario vigente **29-abr-2026** | ✅ |
| GrabrFi USDT withdraw | 1.1% + $1 | Idéntico (todas las blockchains) | ✅ |
| Payoneer ACH in | 1% | 1% eCheck/ACH clientes US | ✅ |
| Payoneer retiro USD→USD | $1.50 fijo | $1.50 con <$50k/mes; 0.5% si >$50k/mes | ✅ |

**Nota menor (no bloqueante):** el modelo no contempla el tramo **0.5% para volumen >$50.000/mes** de Payoneer. Solo importa si operás ese volumen; a $50k el 0.5% son $250 vs $1.50, así que la ruta R2 quedaría fuertemente sobrestimada en ese escenario.

---

## 2. Hallazgos que requieren tu decisión

### 🔴 H1 — La versión HTML no tiene la ruta R5 (Binance P2P)

`CommissionTracker.html` tiene **0 referencias** a Binance. Solo calcula R1–R4:

```js
const paths={astropay:r1,payoneer:r2,grabrfi:r3,santander:r4};
const order=['astropay','payoneer','grabrfi','santander'];
```

No fetchea `/api/binancep2p/usdt/ars/1` ni tiene `binanceUsdtToArs` en `DEFAULTS`.
La app React sí la tiene. `CLAUDE.md` documenta 5 rutas para **ambas** versiones.

**Impacto:** con las tasas de referencia, R5 rinde ~1485 ARS/USD y queda **segunda**, arriba de R2 (Payoneer). Quien use el HTML ve una recomendación incompleta.

**Cambio propuesto:** portar R5 al HTML (fetch de binancep2p + cálculo + card), replicando la lógica de `useCommissionData.ts`.

---

### 🟠 H2 — Belo ACH in 0.3%: podría ser 0% hoy

Belo publicó "Chau comisiones: recibir dólares con belo ahora no tiene costo" y tiene un artículo de T&C específico **"Comisiones 0% para cuenta en USD"**. Las fuentes secundarias se contradicen sobre la vigencia: una dice "período promocional hasta fin de 2025", otra "hasta 2026".

No pude resolverlo automáticamente: `belo.app` no está en la allowlist de red de este entorno, así que solo tengo los snippets de búsqueda, no el texto del T&C.

**Impacto si fuera 0%:** R2/R3 mejoran ~0,3% (≈ +4,5 ARS/USD sobre 1502). Cambia el margen entre rutas, no la ganadora (R3 sigue primera).

**Acción propuesta:** abrí [el T&C de Belo](https://help.belo.app/es/articles/10176827-terminos-y-condiciones-comisiones-0-para-cuenta-en-usd) y confirmá. Si sigue en 0%, poné `beloAchInPct: 0` y `beloAchInMin: 0` — el panel ⚙️ Mercado ya expone ambos campos, no hace falta tocar código.

---

### 🟠 H3 — El spread de 4% de R4 no está documentado ni auditado

`beloUsdToUsdtSpread: 4` es **la comisión más grande de todo el modelo** y no aparece en la tabla de comisiones de `CLAUDE.md` ni en `AUDITORIA-tasas-y-comisiones.md`. No tiene marca de ✅ Oficial ni ⚠️ Estimado — está sin clasificar.

Es lo que hunde R4: con ese 4%, R4 sale último en todos los montos (1341–1446 ARS/USD). Si el spread real fuera 2%, R4 pasaría a ~1477 en USD 5.000 y competiría con R1. Es decir, **un número no auditado está decidiendo el ranking de una ruta entera**.

**Cambio propuesto:** documentarlo en `CLAUDE.md` como ⚠️ Estimado y medirlo con una operación real. Ya es editable en el panel.

---

### 🟡 H4 — La comisión AstroPay 2.5% se muestra pero no se calcula

`RatesTable` muestra "conversión 2.5%" y "ACH out $3.5" para AstroPay, pero **el cálculo de R1 nunca las aplica**:

```ts
const d = cc - wd - c.astropayReceiveFee;
const r1ARS = d * c.astropayUsdtToArs;   // sin 2.5%
```

Probablemente sea **correcto**: `totalBid` de CriptoYa ya viene neto de comisiones del exchange, así que restar 2,5% otra vez sería contarlo dos veces. Pero la UI sugiere lo contrario y no hay comentario que lo explique.

**Cambio propuesto:** dejar el cálculo como está y agregar en `RatesTable` una nota "ya incluida en la tasa" junto al 2,5%, más un comentario en el código.

---

### 🟡 H5 — `CLAUDE.md` afirma algo que no se cumple

> "Los valores **estimados** son ajustables en el panel ⚙️ Mercado de la UI."

Falso para dos de ellos: **AstroPay conversión 2.5%** y **AstroPay ACH out $3.50** están hardcodeados como literales dentro de `buildCommissions()` y no existen en `MarketConfig` ni en `FEE_FIELDS`. No hay forma de editarlos desde la UI.

**Cambio propuesto:** o bien agregarlos a `MarketConfig` + panel, o corregir la afirmación en `CLAUDE.md`. Dado H4 (no se usan en el cálculo), corregir la doc es lo más honesto.

---

## 3. Verificación técnica

| Chequeo | Resultado |
|---|---|
| `tsc --noEmit` (strict) | ✅ 0 errores |
| Simulación R1–R5 en USD 200 / 400 / 1.000 / 5.000 | ✅ sin NaN, monótona, ganadora estable (R3) |
| Umbral Payoneer <$400 | ✅ el salto $4→$1,50 se dispara donde corresponde |
| Endpoints CriptoYa en vivo | ⚠️ **no verificados** — `criptoya.com` bloqueado por la allowlist de red de este entorno |
| Consistencia HTML ↔ React | ❌ ver H1 |

Simulación (tasas de referencia Belo 1511,14 · AstroPay 1476,67 · Binance 1520 · MEP 1478,02):

```
USD   200:  R1=1434  R2=1461  R3=1499  R4=1342  R5=1476  → R3 · ROI puré 1,42%
USD   400:  R1=1441  R2=1476  R3=1502  R4=1396  R5=1483  → R3 · ROI puré 1,63%
USD 1.000:  R1=1443  R2=1489  R3=1502  R4=1429  R5=1485  → R3 · ROI puré 1,63%
USD 5.000:  R1=1447  R2=1491  R3=1505  R4=1446  R5=1489  → R3 · ROI puré 1,83%
```

**Limitación de esta corrida:** no pude llamar a CriptoYa (dominio fuera de la allowlist). Los números de arriba usan las tasas de referencia de la auditoría de junio, no tasas de hoy. La estructura del parseo no cambió respecto de la última verificación exitosa. Para habilitarlo: Settings → Capabilities → agregar `criptoya.com`.

---

## 4. Cambios aplicados

| # | Cambio | Archivos |
|---|---|---|
| H1 ✅ | Ruta R5 (Binance P2P) portada al HTML: fetch de `binancep2p`, `binanceUsdtToArs` en `DEFAULTS`, cálculo, card de tasa y card de ruta | `CommissionTracker.html` |
| H3 ✅ | Spread de R4 documentado como ⚠️ Estimado + análisis de sensibilidad en comentario y doc | `CLAUDE.md`, `useCommissionData.ts` |
| H4 ✅ | Asterisco y nota al pie en la columna Conversión + comentario explicando por qué no se descuenta | `RatesTable.tsx`, `useCommissionData.ts`, footer del HTML |
| H5 ✅ | Corregida la afirmación falsa sobre qué es editable en el panel; se aclara que el 2,5% y el $3,50 de AstroPay son solo informativos | `CLAUDE.md` |
| — ✅ | Script de verificación determinista | `scripts/verify-rates.mjs` (nuevo) |
| H2 ⏳ | **Pendiente tuyo:** confirmar si Belo sigue en 0% ACH y, si es así, cargar `beloAchInPct: 0` / `beloAchInMin: 0` en el panel ⚙️ | — |

### El script `scripts/verify-rates.mjs`

```bash
node scripts/verify-rates.mjs            # tasas en vivo
node scripts/verify-rates.mjs --offline  # sin red, tasas de referencia
node scripts/verify-rates.mjs --json     # salida diffeable entre corridas
```

Chequea cuatro cosas y **sale con código 1 si alguna falla**, para que la tarea programada lo detecte sin interpretación:

1. Cada endpoint de CriptoYa responde y el campo que parsea la app sigue existiendo (detecta cambios de esquema).
2. Las comisiones de `DEFAULTS` en **ambas versiones** coinciden con `EXPECTED_FEES`.
3. Ambas versiones conocen `binanceUsdtToArs` — o sea, detecta exactamente el drift de H1 si vuelve a pasar.
4. Las 5 rutas en 5 montos: sin NaN, sin negativos, y ninguna rinde más que la mejor tasa disponible (atrapa comisiones con signo invertido).

Cuando cambies una comisión a propósito, actualizá `EXPECTED_FEES` en el script.

---

## 5. Push a GitHub — tenés que hacerlo vos

No pude commitear desde el entorno de Cowork: el mount del repo **no permite borrar archivos dentro de `.git`**, y quedó un `.git/index.lock` huérfano de mi `git status` que bloquea toda escritura de git. Las ediciones de archivos sí se aplicaron bien.

Desde una terminal en `D:\Desarrollos\Billeteras Virtuales Argentina`:

```bash
del .git\index.lock

node scripts/verify-rates.mjs                    # deberia salir sin diffs
cd commission-tracker-app && npx tsc --noEmit && cd ..

git add CommissionTracker.html CLAUDE.md scripts/verify-rates.mjs REVISION-2026-08-07.md commission-tracker-app/src
git commit -m "Porta R5 (Binance P2P) al HTML; aclara fees informativos de AstroPay; agrega verify-rates"
git push
```

GitHub Pages se actualiza solo por Actions después del push.

### ⚠️ Ojo: hay dos repos del proyecto

| Ruta | Último cambio |
|---|---|
| `D:\Desarrollos\Billeteras Virtuales Argentina` | commits hasta el 8-jul — **es el que edité** |
| `D:\OneDrive\...\Billeteras\Billeteras Virtuales Argentina` | congelado el 1-jul |

Los dos son repos git independientes con el mismo origin. El de OneDrive está **una semana atrás** y no lo toqué. Si seguís trabajando en los dos vas a tener conflictos; conviene que decidas cuál es el bueno y borres o convertas el otro en un clon.
